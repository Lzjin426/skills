# 任务 +完成

> **前提条件：** 请阅读 `../lark-shared/SKILL.md` 以了解身份验证、全局参数和安全规则。

将任务标记为已完成。

## 推荐命令

```bash
# 完成任务
lark-cli task +complete --task-id "<task_guid>"
```

## 参数

| 参数 | 是否必需 | 描述 |
|-----------|----------|-------------|
| `--task-id <guid>` | 是 | 要完成的任务 GUID。对于飞书任务应用链接，请使用 `guid` 查询参数，而不是 `suite_entity_num` / 显示任务 ID（如 `t104121`）。 |

## 工作流程

1. 确认要完成的任务。
2. 执行命令。
3. 报告成功。

> [!CAUTION]
> 这是一个 **写入操作** —— 执行前必须确认用户意图。